
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ article.title }}</title>
    {{> head }}
</head>
<body>
<h1>{{ article.title }}</h1>
<img src="{{ article.imageUrl }}" alt="">
<p>
    {{ article.text }}
</p>
</body>
</html>